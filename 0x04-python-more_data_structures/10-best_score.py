#!/usr/bin/python3
def best_score(a_dictionary):
    if a_dictionary == {} or is None:
        return None
    biggest = max(a_dictionary.values))
    for key, value in a_dictionary.items():
        if values = biggest:
            return key
